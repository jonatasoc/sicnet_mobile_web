import styled from 'styled-components';

export const Title = styled.h2`
  color: var(--color-text-in-primary);
  font-size: 1.2rem;
  text-align: center;
  margin-bottom: 25px;
  margin-top: -25px;
`;
